
init(
  # use helpful, in this case knw the keys
  data = cdisc_dataset("ADSL", ADSL),
  modules,
  title = NULL,
  filter = list(),
  header = tags$p("Add Title Here"),
  footer = tags$p("Add Footer Here"),
)